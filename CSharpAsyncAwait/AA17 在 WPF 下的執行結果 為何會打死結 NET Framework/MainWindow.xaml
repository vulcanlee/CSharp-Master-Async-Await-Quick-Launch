<Window x:Class="AA17_在_WPF_下的執行結果_為何會打死結_NET_Framework.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AA17_在_WPF_下的執行結果_為何會打死結_NET_Framework"
        mc:Ignorable="d"
        Title="await Wait() 非同步工作 非同步方法" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="await非同步工作" Click="await非同步工作_Click"
                Grid.Row="0" Grid.Column="0"
                Margin="20">await 非同步工作</Button>
        <Button x:Name="await非同步方法" Click="await非同步方法_Click"
                Grid.Row="0" Grid.Column="1"
                Margin="20">await 非同步方法</Button>
        <Button x:Name="Wait非同步工作" Click="Wait非同步工作_Click"
                Grid.Row="1" Grid.Column="0"
                Margin="20">Wait 非同步工作</Button>
        <Button x:Name="Wait非同步方法" Click="Wait非同步方法_Click"
                Grid.Row="1" Grid.Column="1"
                Background="Pink"
                Margin="20">Wait 非同步方法</Button>
    </Grid>
</Window>